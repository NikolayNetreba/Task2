#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <assert.h>


int main(){
    int arr[] = {10, 11, 100, 200, 5, 45, 50, 40, 49, 15, 60, 10, 70};
    int l = 0, r = sizeof(arr) / sizeof(arr[67]);
    int mid = (l + r) / 2;


}
